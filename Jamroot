
import path ;
import regex ;
import string ;
import testing ;
import feature : feature ;

import tbs ;

project TBS-Devices 
	:
		requirements
		<cxxflags>"-std=c++0x -U__STRICT_ANSI__"
	;

path-constant TBS : . ;
path-constant TSERIES-LIBS : TSeries_Libs ;
path-constant TSERIES-PLUGINS : TSeries_Plugins ;

path-constant ROOT-DIR : . ; 

explicit face ;


alias win
	:
		TBS
		TBS.Services
	;

alias all
	:
		TBS
		TBS.Services
		HAL.API
		HAL
		HAL.Client
		HAL.CvClient
	;


include $(TBS)/BJam/libraries.jam ;
include $(TBS)/BJam/device.jam ;

